FC = "$(brew --prefix gcc)/bin"
F77 = "$(brew --prefix gcc)/bin"
FLIBS = -L$(shell brew --prefix gcc)/lib/gcc/current -lgfortran -lquadmath -lm
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS) $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
